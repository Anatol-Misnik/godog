#-----------------------------------------------
# introduction of automation v2
# Copyleft (c) by Anatoliisnik 2022
#-----------------------------------------------

name: env-extraction-test
env:
  COMMIT_HASH: "automation-commit-version-${{ github.sha }}"
on:
  push:
    branches:
      - test
jobs:
  automation-result:
    runs-on: ubuntu-latest
#    needs: [printing-hello]
    steps:
      - name: Set outputs
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
      - name: Check outputs
        run: echo ${{ steps.vars.outputs.sha_short }}       

      - name: Print Result
        run: |
          echo "ENDPOINT: $AE_ENDPOINT"
          echo "CLIENT: ${{ env.AUTH0_CLIENT }}"
          echo "Commit hash: ${{ env.COMMIT_HASH }}"
          echo "Short_hash: $GITHUB_OUTPUT"
